{
  "name": "<%= name %>",
  "version": "0.0.1",
	"description": "<%= description %>",
	"private": true,
  "scripts": {
		"dev": "mono dev",
		"start": "cross-env NODE_ENV=production mono",<% if (eslint === 'yes') { %>
    "lint": "eslint src/ conf/ test/",<% } %>
    "test": "<% if (eslint === 'yes') { %>npm run lint && <% } %>nyc ava --verbose --serial --fail-fast test/ && nyc report --reporter=html",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "nyc": {
    "include": [
      "src/"
    ]
  },
  "license": "MIT",
  "dependencies": {
		"@terrajs/mono": "^0.8.0",<% if (mongodb === 'yes') { %>
		"mongodb-utils": "^1.1.5",<% } if (documentation === 'yes') { %>
		"mono-doc": "^0.5.5",<% } if (mongodb === 'yes') { %>
    "mono-mongodb": "^2.3.1",<% } %>
    "cross-env": "^5.1.1"
	},
  "devDependencies": {
    "mono-test-utils": "^1.2.3",
    "ava": "^0.24.0",
    "codecov": "^3.0.0",<% if (eslint === 'yes') { %>
    "eslint": "^4.13.1",<% } %>
    "nyc": "^11.3.0"
  }
}
